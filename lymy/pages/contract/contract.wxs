var putil = {
  toFix: function (value){
	if(!value){
		return value;
	}
	  if(!isNaN(value)){
		 return value.toFixed(0);//此处2为保留两位小数
	  }
	return value;
  }
}
var msg ="this is abc";
module.exports = {
  toFix: putil.toFix,
  msg:msg
}